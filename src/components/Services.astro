---
const services = [
  {
    icon: "sparkles-outline",
    title: "Automatización con IA",
    desc: "Integro modelos de lenguaje (OpenAI, Claude) en tus procesos operativos para automatizar tareas repetitivas, clasificar información y generar respuestas inteligentes.",
    tags: ["OpenAI", "Claude API", "Agentes IA", "n8n"],
    accent: "indigo",
  },
  {
    icon: "server-outline",
    title: "Desarrollo Backend & APIs",
    desc: "Diseño y construyo APIs RESTful robustas, escalables y bien documentadas con Node.js/NestJS o Python/FastAPI. Arquitectura limpia, SOLID y orientada a producción.",
    tags: ["Node.js", "NestJS", "FastAPI", "PostgreSQL", "Docker"],
    accent: "violet",
  },
  {
    icon: "chatbubbles-outline",
    title: "Chatbots & Integraciones",
    desc: "Desarrollo chatbots funcionales para WhatsApp Business API con flujos conversacionales, lógica de negocio personalizada y conexión a tus sistemas existentes.",
    tags: ["WhatsApp API", "Chatbot", "Node.js", "Webhooks"],
    accent: "emerald",
  },
  {
    icon: "layers-outline",
    title: "Plataformas SaaS & Fullstack",
    desc: "Desarrollo plataformas web completas multi-tenant: desde la arquitectura hasta el despliegue. POS, dashboards administrativos, sistemas de gestión y más.",
    tags: ["React", "Next.js", "NestJS", "PostgreSQL", "AWS"],
    accent: "orange",
  },
];

const accentBorder: Record<string, string> = {
  indigo: "hover:border-indigo-500/40",
  violet: "hover:border-violet-500/40",
  emerald: "hover:border-emerald-500/40",
  orange: "hover:border-orange-500/40",
};

const accentIcon: Record<string, string> = {
  indigo: "text-indigo-400 bg-indigo-500/10",
  violet: "text-violet-400 bg-violet-500/10",
  emerald: "text-emerald-400 bg-emerald-500/10",
  orange: "text-orange-400 bg-orange-500/10",
};
---

<section id="services" class="py-16 sm:py-24 px-4 sm:px-6">
  <div class="max-w-4xl mx-auto">
    <p
      class="text-xs font-semibold uppercase tracking-widest text-accent mb-3"
    >
      Servicios
    </p>
    <h2
      class="text-2xl sm:text-3xl lg:text-4xl font-bold tracking-tight text-balance text-fg mb-3 sm:mb-4"
    >
      ¿En qué puedo ayudarte?
    </h2>
    <p class="text-muted mb-8 sm:mb-12 text-sm sm:text-[15px] max-w-2xl text-pretty">
      Cuatro frentes en los que puedo entrar a tu proyecto, solo o integrado a tu
      equipo.
    </p>

    <!-- 1 col mobile → 2 col tablet → 2 col desktop (2x2) -->
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 sm:gap-5">
      {
        services.map((service) => (
          <div
            data-anim="service-card"
            data-spotlight
            class={`group bg-fg/[.03] border border-line/10 rounded-2xl p-5 sm:p-6 transition-all duration-300 hover:-translate-y-1 ${accentBorder[service.accent]}`}
          >
            <div
              class={`w-10 h-10 rounded-xl flex items-center justify-center mb-4 sm:mb-5 transition-transform duration-300 group-hover:scale-110 ${accentIcon[service.accent]}`}
            >
              <ion-icon name={service.icon} class="text-xl" />
            </div>
            <h3 class="text-base font-semibold text-fg mb-2 sm:mb-3">
              {service.title}
            </h3>
            <p class="text-sm text-muted leading-relaxed mb-4 sm:mb-5">
              {service.desc}
            </p>
            <div class="flex flex-wrap gap-1.5">
              {service.tags.map((tag) => (
                <span class="px-2 py-0.5 text-xs text-faint bg-fg/[.03] border border-line/[.08] rounded-md">
                  {tag}
                </span>
              ))}
            </div>
          </div>
        ))
      }
    </div>

    <div class="mt-7 sm:mt-8 text-center">
      <a
        href="#contact"
        class="inline-flex items-center gap-2 text-sm text-muted hover:text-fg transition-colors duration-200"
      >
        Cuéntame qué necesitas →
      </a>
    </div>
  </div>
</section>
